# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# The pod runs three containers sharing the network namespace:
#   * locust-master: Python locust master, serves --class-picker UI on :8089
#     and accepts worker connections on :5557. Aggregates stats from both
#     workers and exposes them at /metrics on :8000.
#   * locust-python-worker: Python locust worker for the non-glutton tests.
#     LOCUST_NO_GLUTTON_USER=1 prevents tests/glutton.py from declaring the
#     stub User on this worker so GluttonUser spawns are owned exclusively
#     by the boomer container.
#   * boomer-glutton: Go re-implementation of GluttonUser. Connects to the
#     master at localhost:5557 via the locust worker ZMQ protocol; exposes
#     its own per-worker diagnostics at /metrics on :8001 (aggregate stats
#     flow through the master via boomer.RecordSuccess). Omitted when
#     deploy.sh is run with --no-boomer, leaving two containers — see the
#     marker comments around it for why that matters.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: locust
  namespace: benchmarking
spec:
  replicas: 1
  selector:
    matchLabels:
      app: locust
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: locust
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
    spec:
      containers:
      - name: locust-master
        image: us-docker.pkg.dev/${PROJECT_ID}/gcr.io/ate-images/locust-test:latest
        imagePullPolicy: Always
        args:
        - "-m"
        - "locust"
        - "-f"
        - "/app/tests"
        - "--master"
        - "--master-bind-host=0.0.0.0"
        - "--class-picker"
        env:
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://opentelemetry-collector.gke-managed-otel.svc.cluster.local:4317
        ports:
        - name: prometheus
          containerPort: 8000
          protocol: TCP
        - name: web
          containerPort: 8089
          protocol: TCP
        - name: zmq
          containerPort: 5557
          protocol: TCP
        volumeMounts:
        - name: servicedns-ca
          mountPath: /run/servicedns-ca
          readOnly: true
        - name: podidentity
          mountPath: /run/podidentity.podcert.ate.dev
          readOnly: true
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"

      - name: locust-python-worker
        image: us-docker.pkg.dev/${PROJECT_ID}/gcr.io/ate-images/locust-test:latest
        imagePullPolicy: Always
        args:
        - "-m"
        - "locust"
        - "-f"
        - "/app/tests"
        - "--worker"
        # Literal IPv4, not "localhost". The master binds 0.0.0.0:5557, which
        # is IPv4-only; locust sets ZMQ_IPV6 on the worker socket whenever the
        # master host resolves to anything AF_INET6, and the pod's /etc/hosts
        # maps localhost to ::1 as well as 127.0.0.1. The worker would then
        # dial [::1]:5557 and retry forever against a silent master.
        - "--master-host=127.0.0.1"
        env:
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://opentelemetry-collector.gke-managed-otel.svc.cluster.local:4317
        # Skips the GluttonUser stub declaration so the master never assigns
        # GluttonUser spawns to this worker — boomer owns that class.
        - name: LOCUST_NO_GLUTTON_USER
          value: "1"
        volumeMounts:
        - name: servicedns-ca
          mountPath: /run/servicedns-ca
          readOnly: true
        - name: podidentity
          mountPath: /run/podidentity.podcert.ate.dev
          readOnly: true
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"

      # BEGIN boomer
      # deploy.sh --no-boomer strips everything between these markers with a
      # sed range delete. The marker comments, not the container name, are what
      # define the range — keep them wrapped tightly around this one container.
      #
      # Strip it for any run that is not a glutton benchmark. boomer's
      # sumUsersAmount (vendor/github.com/myzhan/boomer/runner.go) sums the
      # counts in a spawn message and discards the class names, so this
      # container answers spawns for *any* user class the master assigns it and
      # runs glutton instead. spawnComplete echoes the original class counts
      # back verbatim, so the web UI reports the class you picked either way and
      # the only tell is glutton's rows in the stats table.
      - name: boomer-glutton
        # Same image as the master/worker — the boomer-glutton Go binary is
        # baked in at /app/boomer-glutton by benchmarking/locust/Dockerfile.
        image: us-docker.pkg.dev/${PROJECT_ID}/gcr.io/ate-images/locust-test:latest
        imagePullPolicy: Always
        command: ["/app/boomer-glutton"]
        args:
        - "--prometheus-addr=:8001"
        # Fetch runtime config (trace probability, wait-time bounds) from
        # http://{master-host}:{master-web-port}/boomer-config on each
        # spawn message so values the operator types into the web UI form
        # are honored. The endpoint is served by
        # benchmarking/locust/common/boomer_config.py on the master.
        - "--master-web-port=8089"
        env:
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: opentelemetry-collector.gke-managed-otel.svc.cluster.local:4317
        ports:
        - name: boomer-prom
          containerPort: 8001
          protocol: TCP
        volumeMounts:
        - name: servicedns-ca
          mountPath: /run/servicedns-ca
          readOnly: true
        - name: podidentity
          mountPath: /run/podidentity.podcert.ate.dev
          readOnly: true
        resources:
          requests:
            cpu: "500m"
            memory: "256Mi"
      # END boomer

      volumes:
      - name: servicedns-ca
        projected:
          sources:
          - clusterTrustBundle:
              signerName: servicedns.podcert.ate.dev/identity
              labelSelector:
                matchLabels:
                  podcert.ate.dev/canarying: live
              path: ca.crt
      # The benchmark's own client identity, presented to ateapi. Its
      # interceptor rejects calls carrying no credential, taking identity from
      # an mTLS client cert or, failing that, a Bearer token. Use the cert:
      # that is what the base install gives ate-controller and atenet-router
      # (the token path is the opt-in ATE_ATEAPI_CLIENT_AUTH=token overlay),
      # and it yields a SPIFFE ID rather than an opaque JWT subject.
      - name: podidentity
        projected:
          sources:
          - podCertificate:
              signerName: podidentity.podcert.ate.dev/identity
              keyType: ECDSAP256
              credentialBundlePath: credential-bundle.pem
---
apiVersion: v1
kind: Service
metadata:
  name: locust
  namespace: benchmarking
spec:
  selector:
    app: locust
  ports:
  - name: web
    protocol: TCP
    port: 8089
    targetPort: 8089
  - name: prometheus
    protocol: TCP
    port: 8000
    targetPort: 8000
  - name: boomer-prom
    protocol: TCP
    port: 8001
    targetPort: 8001
